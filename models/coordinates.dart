class Coordinates {
  /// An x-coordinate on the board (integer).
  final int x;

  /// A y-coordinate on the board (integer).
  final int y;

  Coordinates({
    required this.x,
    required this.y,
  });
}